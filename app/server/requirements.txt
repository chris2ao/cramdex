fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
# starlette.testclient prefers httpx2 (its own successor package) over httpx
# and emits a StarletteDeprecationWarning when only httpx is installed; this
# silences that warning without any code change, per the warning's guidance.
httpx2>=2.0
pytest>=8.0
pytest-asyncio>=0.24
pyyaml>=6.0
anthropic>=0.69
